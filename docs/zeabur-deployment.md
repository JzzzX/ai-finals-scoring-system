# Zeabur 外部评审版 · 部署与运维

## 2026-09-23 晚间设计版发布（当前状态）

- 代码提交 `06305b85f23a0a0e37167bb6a1f686ca3d3f5bd5` 已推送 GitHub `main` 及 `codex/refined-judge-experience`；未推送 Gitea。
- 从该提交的 `git archive` 发布至已有 scoring 服务，部署 ID `6ab3c0965d7569a2d1c6e403`，状态 `RUNNING`。当前为代码包部署，GitHub 推送不会自动触发云端发布。
- 评委通过首页姓名选择及本人确认进入；管理员使用独立账密入口。下方早期账密版记录为历史状态。
- 本次更新统一评委与管理员视觉、加入吉祥物、调整评分区配色，修复全屏榜单底色，并增加前三名展示与 PNG 榜单导出；保留 CSV 导出。
- 发布前线上一致性备份：`/data/backups/before-refined-design-20260923-2006.sqlite`。备份及发布后数据库 `integrity_check=ok`；9 个账号、12 支队伍、0 份评分、赛事 `draft` 保持一致。
- 本地构建与 14 项测试通过。公网健康检查 200、评委名单 8 人、未登录管理员成绩接口 401；线上 `index-D9C225lO.js`、`index-Cy2myaD3.css` 和吉祥物图片与本地构建逐字节一致。浏览器实际渲染正常，未观察到控制台错误。未向正式库写入测试评分。

GitHub 维护外部评审版（评委姓名选择、管理员账密登录），Gitea 独立维护公司内部飞书版。本次只发布到 GitHub 和已有 Zeabur 服务，不向 Gitea 推送。保留最新评分提交修复、SQLite 持久化和备份恢复保护。

## 登录与数据

- 评委在首页选择本人姓名并确认后进入评分；管理员使用独立账密入口。不要求飞书身份，不开放自行注册。姓名选择不验证本人身份，仅签发评委权限会话。
- `AUTH_MODE=local`，页面不显示飞书登录或通讯录授权，飞书相关 API 返回 404。
- 普通评委由管理员填写姓名与职务创建，系统生成内部账号标识；评委无需领取密码。管理员和评委分别授予角色，管理员不会自动获得评分资格。
- 密码至少 12 位，数据库存储加盐 scrypt 哈希；正式密码不进入代码、日志或公开文档。
- 管理员可创建、停用账号、修改角色和重置密码；重置密码会撤销该用户全部旧会话，已有评分保留。
- 外部版未迁入内网版旧评分，保留 12 支队伍。2026-09-23 晚间发布验证时已配置 8 位评委、1 位管理员，赛事为 draft、0 份评分；实时状态以网站为准。
- 原公司内网服务、数据和飞书回调均不修改。旧飞书文档仅供内部版参考。

## 服务设置

- 根目录 Dockerfile 构建 React 和 NestJS，Node.js 24，单副本。
- `scoring-data` 持久化卷挂载 `/data`，数据库 `/data/finals.sqlite`。
- 镜像默认端口 3001；当前平台注入 `PORT=8080`，HTTP 路由为 8080。应用和健康检查均读取 PORT。
- `HOST=0.0.0.0`、`NODE_ENV=production`、`COOKIE_SECURE=true`、`AUTH_MODE=local`。
- `APP_ORIGIN=https://gambol-ai-finals-0928.preview.aliyun-zeabur.cn`。
- PID 文件 `/tmp/finals.server.pid`，启动时清理旧 PID，不随数据库持久化。
- 不需要 FEISHU_APP_ID、FEISHU_APP_SECRET、FEISHU_REDIRECT_URI、FEISHU_TENANT_KEY 或 scope。
- 不把 .env、数据库、日志、密码分发清单或备份放入 Git / 镜像。

## 已有云资源

- 服务器：Aliyun Beijing 2C 4GB，ID `6ab375d188bd3c746fd58ab9`。
- 项目：ai-finals-scoring，ID `6ab379ce76ea2bdcc9db9840`。
- 环境：`6ab379ce36d2a6cac4934dc0`。
- 服务：scoring，ID `6ab379f376ea2bdcc9db9857`。
- 入口：https://gambol-ai-finals-0928.preview.aliyun-zeabur.cn
- 控制台：https://zeabur.com/projects/6ab379ce76ea2bdcc9db9840/services/6ab379f376ea2bdcc9db9857?envID=6ab379ce36d2a6cac4934dc0
- 用户已完成服务器付款、预备案域名实名认证及绑定。服务器按月付费并可能自动续费；赛后先导出备份，再处理停用。

## 发布与验证

### 网站图标更新（2026-09-23）

提交 `9cd9a81` 增加墨绿与香槟金的奖杯爪印 SVG 网站图标。部署 `6ab3c4ac5d7569a2d1c6e4dd` 已运行；首页图标引用、图标响应类型和内容、健康检查均已核对。当前代码包发布不绑定 GitHub 自动部署。

### 早期部署记录（历史快照）

部署到上述已有服务，不重复创建服务器或新数据库。平台变量修改后须重启并验证运行时实际值。

已完成：生产账密 API 测试（关闭 OAuth、管理员创建评委、角色隔离、CSRF、密码重置及旧会话撤销），全部系统测试及构建。50 位评委 × 12 队的本地并发写入测试无丢分；不能把本地耗时当作生产性能承诺。

2026-09-23 外部版发布：代码提交 `9048cba`，云端部署 `6ab381f65d7569a2d1c6d27d`。9 项测试全部通过。公网已验证实际管理员账密登录成功、角色仅为 admin、成绩和成员接口可读、退出后旧会话返回 401、OAuth 登录接口返回 404。运行时为 AUTH_MODE=local、COOKIE_SECURE=true、PORT=8080。前端静态资源与本次构建一致。

Chrome 实际渲染已检查：登录页非空、无框架错误、控制台检查无异常；错误账号提交后显示明确提示。浏览器视口覆盖未生效，因此不把该截图当作手机真机验证。手机流量及评委实际登录仍需在名单配置后演练。

云端已创建一个独立随机密码的赛事管理员，仅授予 admin；密码只交付至维护者本机的私有文件。当前 0 位评委、0 份评分，赛事 draft。评委名单未提供，不预建虚构评委，不提前开始评分。

切换前一致性备份位于 `/data/backups/before-password-version-20260923.sqlite`，原云配置也已在维护者本机私有目录备份。没有修改 Gitea 或内部服务器。

基础云环境已验证：域名、HTTPS 证书、GET 首页和健康检查 200、未登录数据接口 401、重启后持久化检查文件保留、SQLite integrity_check 为 ok。验证用检查文件已移除。手机流量和正式评委设备仍需演练。

当前电脑代理曾将域名解析至 198.18.* 导致 TLS 异常。保持域名与证书校验、仅定向已购服务器 IP 的请求成功，北京服务器正常 DNS 请求也成功；未修改全局代理或 DNS。

## 备份与比赛操作

```sh
npm run db:backup -- /data/backups/before-finals.sqlite
```

在线备份使用 SQLite backup API，不只复制运行中的主数据库文件。恢复前先停止服务，使用现有恢复脚本；恢复会清除会话。正式库不写测试分数。

1. 用赛事管理员账号登录，进入“成员与权限”创建评委。
2. 提醒评委仅选择本人姓名；管理员账号密码由主办方单独保管。
3. 核对 12 支队伍、评委人数和角色；在“赛事设置”开始评分，固定名单。
4. 评委用手机流量登录，提交后以服务端确认成功为准。
5. 结束评分后导出队伍汇总、逐评委明细，另做数据库备份。
